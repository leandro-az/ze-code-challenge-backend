import { expect } from 'chai';
import { describe, it } from 'mocha';

describe('TESTES PROTOT', () => {
    describe('TESTE ONE', () => {
        it('should return 1', () => {
            expect(1).to.eq(1);
        });
    });
    describe('TESTE TWO', () => {
        it('should return 2', () => {
            expect(2).to.eq(2);
        });
    });
    describe('TESTE THREE', () => {
        it('should return 3', () => {
            expect(3).to.eq(3);
        });
    });
});
